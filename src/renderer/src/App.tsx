/**
 * A placeholder, and deliberately an ugly one.
 *
 * Its whole job is to prove the chain: TypeScript compiled, React mounted, the page loaded inside a
 * window that allows it, and reload working while you edit this file. There is no styling because
 * the styling foundation is a separate piece of work, and there is no welcome text worth the name
 * because the welcome page is too.
 *
 * So an unstyled heading is the honest state of this application right now. It should look raw
 * until Mantine and Sass arrive, because anything else would be a hand made version of what the
 * component library is for.
 */
export function App() {
  return (
    <main>
      <h1>FreeHarmony</h1>
      <p>The window opens, the build works, and nothing else has been built yet.</p>
      <p>No remote is involved. Nothing has been read.</p>
    </main>
  );
}
